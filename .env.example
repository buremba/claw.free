BASE_URL=http://localhost:8788
# Public frontend link shown on landing page / FAQ (examples: @ClawFreeBot or https://t.me/ClawFreeBot)
VITE_TELEGRAM_BOT_URL=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
DATABASE_URL=postgres://postgres:postgres@localhost:5432/claw_free
# Required in production. In local development, the API falls back to
# a built-in insecure secret if this is not set.
COOKIE_SECRET=

# Relay tunnel — set RELAY_URL to your Railway public domain so bot VMs
# can connect back and Telegram webhooks are forwarded through the tunnel.
# Falls back to BASE_URL if not set.
RELAY_URL=

# Optional: protects GET /relay/status endpoint
INTERNAL_API_KEY=

# Telegram Mini App — bot token used to validate Telegram WebApp initData
TELEGRAM_BOT_TOKEN=

# --- Agent provider: Railway (preferred) ---
# When all three are set, agents are deployed as separate Railway services.
# Each agent gets its own public URL — no relay tunnel needed.
RAILWAY_API_TOKEN=
RAILWAY_PROJECT_ID=
RAILWAY_AGENT_IMAGE=

# --- Agent provider: GCP (fallback) ---
# Platform-managed GCP — service account for creating VMs on behalf of
# Mini App users. Used when Railway provider is not configured.
GCP_SERVICE_ACCOUNT_KEY=
GCP_PROJECT_ID=

# Optional: Headscale overlay network (alternative to relay tunnel, GCP only)
HEADSCALE_URL=
HEADSCALE_API_KEY=
