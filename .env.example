BASE_URL=http://localhost:8788
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
DATABASE_URL=postgres://postgres:postgres@localhost:5432/claw_free
# Required in production. In local development, the API falls back to
# a built-in insecure secret if this is not set.
COOKIE_SECRET=

# Relay tunnel — set RELAY_URL to your Railway public domain so bot VMs
# can connect back and Telegram webhooks are forwarded through the tunnel.
# Falls back to BASE_URL if not set.
RELAY_URL=

# Optional: protects GET /relay/status endpoint
INTERNAL_API_KEY=

# Telegram Mini App — bot token used to validate Telegram WebApp initData
TELEGRAM_BOT_TOKEN=

# Mode A (platform-managed GCP) — service account for creating VMs on
# behalf of Mini App users. Not needed for Mode B (user's own GCP).
GCP_SERVICE_ACCOUNT_KEY=
GCP_PROJECT_ID=

# Optional: Headscale overlay network (alternative to relay tunnel)
HEADSCALE_URL=
HEADSCALE_API_KEY=
